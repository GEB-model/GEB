# Variables

Many modules in GEB have a set of state-variables that are saved in MODULE-NAME.var.VARIABLE-NAME.

These variables can be for example numpy arrays, geodataframes, scalars etc. These variables are saved at the end of the spinup and automatically restored at the start of a run.